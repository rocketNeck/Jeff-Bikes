<%= form_for [@bike, Comment.new] do |f| %>
  <p><%= f.label :content, "New Comment"%></p>
  <p><%= f.text_area :content, class:"form-control", rows:"3"%></p>
  <p><%= f.submit "Add Comment", class:"btn btn-info" %></p>
<% end %>

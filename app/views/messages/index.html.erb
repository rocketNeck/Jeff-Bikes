<div class="row">
  <div class="col-md-3 text-center">
    <%= image_tag avatar_url(@other), class: "imag-circle avatar-medium"%><br>
    <strong><%= @other.fullname %></strong>
    <%= link_to "View Profile", @other, class: "btn btn-default wide"%>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">
      <div class="panel-heading">
        Conversation with <%= @other.fullname %>
      </div>
      <div class="panel-body">
        <div class="container">

          <%= form_for [@conversation, @conversation.messages.new] do |f| %>
            <div class="form-group">
              <%= f.text_area :content, id: 'new_message', placeholder: "Add a personal message", class: "form-control" %>
            </div>
            <%= f.hidden_field :user_id, value: current_user.id %>

            <div class="actions">
              <%= f.submit "Send Message", class: "btn btn-primary" %>
            </div>
          <% end %>

        </div>
      </div>
    </div>

    <div id="chat">
      <%= render 'messages' %>
    </div>
  </div>
</div>
<script type="text/javascript" charset="utf-8">
  $(function() {
    $('form').submit(function(e){
      e.preventDefault();

      var values = $(this).serialize();
      var posting = $.post(this.action, values);

      posting.done(function(data) {
        console.log(data);
      })
    })
  })

</script>


<!-- <form class="new_message" id="new_message" action="/conversations/3/messages" accept-charset="UTF-8" method="post"><input name="utf8" type="hidden" value="✓"><input type="hidden" name="authenticity_token" value="xx">
            <div class="form-group">
              <textarea id="new_message" placeholder="Add a personal message" class="form-control" name="message[content]"></textarea>
            </div>
            <input value="3" type="hidden" name="message[user_id]" id="message_user_id">

            <div class="actions">
              <input type="submit" name="commit" value="Send Message" class="btn btn-primary" data-disable-with="Send Message">
            </div>
</form> -->
